# Архитектура хранения: карта процесса

## Процесс от покупки до выдачи

```
Клиент покупает на сайте
        ↓
Worker генерирует натальную карту (CSV)
        ↓ сохраняет в
GDrive: Клиенты/Разборы/<Имя>_<contract12>_<YYYYMMDD>/karta_<contract12>.csv
        ↓
pull_client.py (skill 1) скачивает
        ↓ создаёт локально
D:\...\Купившие разбор\<Имя>_<contract12>_<YYYYMMDD>\karta_<contract12>.csv
        ↓
Анализ (skill 2)
        ↓ создаёт локально
<Имя>_<DDMMYYYY>_миссия.md + summary.md
        ↓
Параллельно:
  • Image (skill 4) → Generated_image.png + cover.webp     [локально]
  • PDF   (skill 5) → <Имя>_<DDMMYYYY>_миссия.pdf          [локально]
        ↓
Delivery (skill 6):
  → GDrive (Разборы/<folder>/): mission_<email>_<date>.pdf + _cover.png
  → R2: mission/<contract12>/cover.webp + summary.html
  → Worker обновляет Sheet, отправляет email + WhatsApp
        ↓
Git commit (только текстовые: .md + .csv) + push в конце сессии
```

## Что где хранится (по хранилищам)

### D:\DariaGalactic\Профайлы клиентов\ (локальный диск)

Полное рабочее хранилище. Всё что создаётся в процессе — здесь.

| Файл | Создаёт | Остаётся навсегда |
|---|---|---|
| `karta_<id>.csv` | pull_client.py (из GDrive) | да |
| `<Имя>_<DDMMYYYY>_миссия.md` | skill 2 (analysis) | да |
| `summary.md` | skill 2 (analysis) | да |
| `Generated_image.png` | skill 4 (image) | да |
| `cover.webp` | skill 4 (image) | да |
| `<Имя>_<DDMMYYYY>_миссия.pdf` | skill 5 (pdf) | да |

### Git (GitHub: IntergalacticClientsProfiles)

Бэкап + версионирование текстовых файлов. Push после каждой доставки. Бинарники (PDF, PNG, WEBP) исключены `.gitignore` — они хранятся только на D:\ и GDrive.

| Файл | Зачем |
|---|---|
| `karta_<id>.csv` | исходная карта |
| `<Имя>_<DDMMYYYY>_миссия.md` | полный разбор |
| `summary.md` | саммари |

### Google Drive (gdrive:DariaGalactic/Клиенты/Разборы/<folder>/)

Файлы для личного кабинета клиента.

| Файл | Создаёт | Зачем |
|---|---|---|
| `karta_<id>.csv` | Worker (при покупке) | исходная карта |
| `mission_<email>_<date>.pdf` | deliver_mission.py | PDF разбора для клиента |
| `mission_<email>_<date>_cover.png` | deliver_mission.py | обложка ДНК для клиента |

### Cloudflare R2 (через Worker API)

Данные для веб-кабинета на сайте. Загружает `deliver_mission.py`.

| Файл | Зачем |
|---|---|
| `mission/<id>/cover.webp` | обложка в кабинете |
| `mission/<id>/summary.html` | саммари (парсится из summary.md в HTML) |

## Формат папки клиента

```
Купившие разбор/Имя_contractID_YYYYMMDD/
├── karta_<contractID>.csv
├── <Имя>_<DDMMYYYY>_миссия.md
├── summary.md
├── Generated_image.png
├── cover.webp
└── <Имя>_<DDMMYYYY>_миссия.pdf
```

## Структура на диске D:\

```
D:\DariaGalactic\Профайлы клиентов\
├── Купившие разбор/   ← клиенты с оплаченным разбором
├── Другие/            ← тестовые, ручные, старые n8n-форматы
└── .git/
```

Символические ссылки (C: → D:):
- `Producty\DariaGalactic\Профайлы клиентов\` → `D:\DariaGalactic\Профайлы клиентов\`
- `Producty\DariaGalactic\Аналитика\` → `D:\DariaGalactic\Аналитика\`
- `Producty\DariaGalactic\Источники для анализа\` → `D:\DariaGalactic\Источники для анализа\`
- `Producty\DariaGalactic\Книга и гайды\` → `D:\DariaGalactic\Книга и гайды\`

Git для DariaGalactic (DariaGalacticChakra) — **удалён**. Все файлы дублируются на GDrive, git-история не нужна. Клиентские профайлы имеют отдельный git-репо на D:.

## Критические правила

- Все данные живут на **D:\**, на C: — только код, скрипты, правила
- `rclone copy` — ЕДИНСТВЕННАЯ допустимая команда (НЕ sync, НЕ move)
- Удалять что-либо с GDrive **ЗАПРЕЩЕНО** — привязано к ЛК
- Перед любым переносом → аудит, после → повторный аудит
- Git push после каждой доставки
- **После каждого push** — обязательная очистка C: (git gc, temp, одноразовые скрипты)
- Одноразовые скрипты (audit_*, restore_*, reorganize_*) удалять после использования
